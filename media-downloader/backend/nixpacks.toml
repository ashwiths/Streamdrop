# nixpacks.toml — Railway build configuration
# Installs system packages needed by yt-dlp and ffmpeg.

[phases.setup]
# python3 and ffmpeg: required by yt-dlp to merge video+audio streams
# yt-dlp: native binary from nixpkgs
nixPkgs = ["python3", "ffmpeg", "yt-dlp"]

[start]
cmd = "node server.js"
