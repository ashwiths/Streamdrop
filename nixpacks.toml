providers = ["node"]

[phases.setup]
nixPkgs = ["ffmpeg", "curl"]

[phases.build]
cmds = [
  "cd media-downloader/backend && npm ci",
  "cd media-downloader/backend && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_linux -o yt-dlp",
  "cd media-downloader/backend && chmod a+rx yt-dlp"
]

[start]
cmd = "cd media-downloader/backend && node server.js"
